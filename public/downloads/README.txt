Placez ici le fichier APK nomm� `jodys.apk` pour activer le t�l�chargement
direct depuis la landing (bouton ? /downloads/jodys.apk).

Ou d�finissez `DOWNLOAD_APK_URL` dans Coolify vers une URL externe.
